#include "tokenlist.h"

/**
* @brief Lexes a source file into a list of tokens.
* 
* @param source_file A string containing the source file to be lexed.
* @returns A pointer to the list of tokens.
*/
TokenList *lex(char *source_file);